{
  "extends": "./.nuxt/tsconfig.app.json",
  "compilerOptions": {
    "baseUrl": ".", // important for path aliases
    "paths": {
      "@/*": ["./*"], // map @ to the frontend root
      "~/*": ["./*"] // optional: ~ alias
    }
  }
}
